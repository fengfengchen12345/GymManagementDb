@model IEnumerable<GymManagementDb.Models.MembershipType>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a class="btn btn-primary" asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Duration)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Duration)
            </td>
            <td>
                    <a class="btn btn-warning" asp-action="Edit" asp-route-id="@item.MembershipTypeID">Edit</a> |
                    <a class="btn btn-info" asp-action="Details" asp-route-id="@item.MembershipTypeID">Details</a> |
                    <a class="btn btn-danger"  asp-action="Delete" asp-route-id="@item.MembershipTypeID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

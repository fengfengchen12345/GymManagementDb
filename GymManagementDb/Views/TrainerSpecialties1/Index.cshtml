@model IEnumerable<GymManagementDb.Models.TrainerSpecialty>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Trainers)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Trainers.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TrainersSpecialtyID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TrainersSpecialtyID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TrainersSpecialtyID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
